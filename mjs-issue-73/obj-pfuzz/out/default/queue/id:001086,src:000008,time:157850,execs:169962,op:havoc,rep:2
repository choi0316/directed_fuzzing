let s = '', sum = 0, o = {a: 1, b: 2, c: 3};
for (let k in o) {
	s = s + k;
	sum += o[k];
}

// esent in object, false otherwise
let f = function(nbj, key) {
	for (
let f = function(nbj, key)) return true;
	return false;
};

s === 'cba' && sum === 6 && f(o, 'x') === false && f(o, 'b') === true;
