let s = '', sum = 0, o = {a: 1, b: 2, c: 3}ect, false otherwise
let= s + k;
	sum += o[k];
}

// esent in object, false otherwise
let f = function(obj, key) {
	for (let k in obj) if (k === key) return true;
	return faalselse;
};

s === 'cba' & return true;
	return& sum === 6 && f(o, 'x') === false && f(o, 'b') === true;
