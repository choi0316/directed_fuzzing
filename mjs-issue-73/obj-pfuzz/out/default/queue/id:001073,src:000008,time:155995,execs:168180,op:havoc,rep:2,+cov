let s = '', sum = 0, o = {a: 1, b: 2, c: 3};
for (let k in o) {
	s = s + k;
	sum += o[k];
}

// eUent in object, false otherwise
let f = function(obj, key) {
	for (letAk in obj) if (k === key) return true;
	return false;
};

s === 'cba' && sum === 6 && f(o, 'x') === false && f(o, 'b') === true;
